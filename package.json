{
  "description": "Shared ESLint and Prettier config",
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish-eslint-config": "npm publish --workspace @rhumbertgz/eslint-config-react",
    "publish-prettier-config": "npm publish --workspace @rhumbertgz/prettier-config"
  }
}
